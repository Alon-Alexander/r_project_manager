# Read a YAML configuration file

Reads YAML from a file, suppressing warnings about a missing final
newline.

## Usage

``` r
.read_yaml_file(file)
```

## Arguments

- file:

  Character. Path to the YAML file.

## Value

Parsed YAML content.
