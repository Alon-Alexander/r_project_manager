% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{pm_write_file}
\alias{pm_write_file}
\title{Write an object to a file based on its extension}
\usage{
pm_write_file(file, x, ..., object_names = NULL)
}
\arguments{
\item{file}{Character. Path to the file to write}

\item{x}{The object to write. For tabular formats (CSV, TSV, Parquet), must be
a data.frame or similar tabular object. For RData files, can accept multiple
objects via \code{...}.}

\item{...}{Additional arguments passed to the underlying write function.
For RData files, additional objects can be provided here.}

\item{object_names}{Character vector. Optional. \strong{Internal parameter.} For RData files, explicitly
specify the names for objects. If provided, must match the number of objects
(x + objects in ...). If not provided, names are extracted from the call.
This parameter is intended for use when forwarding a \code{...} call from a parent frame
where object names need to be preserved. See \code{PMData$write()} for example usage.}
}
\value{
Invisibly returns the file path.
}
\description{
Writes an object to a file based on the file extension. Supports multiple file formats
including tabular data (CSV, TSV), serialized R objects (RDS), and
R data files (RData/RData).
}
\details{
Supported file formats:
\itemize{
\item \strong{CSV}: Comma-separated values files (\code{.csv}) - uses \code{write.csv()}. Requires a data.frame.
\item \strong{TSV}: Tab-separated values files (\code{.tsv}) - uses \code{write.table()} with \code{sep = "\\t"}. Requires a data.frame.
\item \strong{Parquet}: Apache Parquet files (\code{.parquet}, \code{.pqt}) - requires the \code{arrow} package. Requires a data.frame.
\item \strong{RDS}: R serialized data files (\code{.rds}) - uses \code{saveRDS()}. Accepts any R object.
\item \strong{RData/RData/Rda}: R data files (\code{.rdata}, \code{.RData}, \code{.rda}, \code{.Rda}) - uses \code{save()}.
Can accept multiple objects: \code{pm_write_file("file.RData", obj1, obj2, obj3)}.
}

For tabular formats (CSV, TSV, Parquet), the function validates that the object
is a data.frame or similar tabular structure.

The \code{object_names} parameter is an internal parameter used when forwarding \code{...} calls
from a parent frame. When a wrapper function (like \code{PMData$write()}) needs to preserve
object names from the original call, it can capture the names and pass them via
\code{object_names}. This ensures that RData files contain objects with the correct names
even when called through wrapper functions. See the implementation of \code{PMData$write()}
for an example of how to use this parameter.
}
\examples{
# Example usage:
withr::with_tempdir({

# Write a data.frame to CSV
df <- data.frame(x = 1:5, y = letters[1:5])
pm_write_file("data.csv", df)

# Write a data.frame to TSV
pm_write_file("data.tsv", df)

# Write any object to RDS
my_list <- list(a = 1, b = 2)
pm_write_file("data.rds", my_list)

# Write multiple objects to RData
obj1 <- data.frame(x = 1:3)
obj2 <- c("a", "b", "c")
pm_write_file("data.RData", obj1, obj2, obj3 = 42)

})

}
